const express = require("express");

checkConfig = 
/**
 * Makes a new Express server for DuktServer (find a better name later :tm:)
 */
DuktServer = class DuktServer {
    constructor(config) {
        this._server = express();
    }
}

module.exports = {
    DuktServer
}